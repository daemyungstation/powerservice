<?xml version="1.0" encoding="UTF-8"?>

<config>
    <!-- 로그인이 필요하지 않은 요청/페이지 리스트  -->
    <actionlist>
        <action path="/login" />
        <action path="/email/recv" />
        <action path="/email/recv-exchange" />
        <action path="/user/user/password/change" />
        <action path="/common/password-change-popup.do" />
        <action path="/file/download" />
        <action path="/total-search/*" />
        <action path="/total-sch/*" />
        
        <action path="/sys/nice-credit/sendSafeKeyIssueSms" />
        <action path="/sys/nice-credit/checkplus_send_result" />
        
        <!-- 파일관리 - 녹취파일 재생용 다운로드 -->
        <action path="/dlw/prod/getWavFile" />
        
    </actionlist>

    <!-- IP 접근/차단 리스트  -->
    <!-- 모드1 : 기본적으로 모두 차단하고 리스트에 있는 IP만 허용  -->
    <!-- 모드2 : 기본적으로 모두 허용하고 리스트에 있는 IP만 차단  -->
    <iplist mode="1">
        <ip>10.20.30.*</ip>
        <ip>*</ip>
    </iplist>
</config>
