<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="DlwExtcQuryMap">
    <select id="DlwExtcQuryMap.chkExtcQury" parameterType="map" resultType="resultMap">
        

        SELECT /*DlwExtcQuryMap.chkExtcQury 쿼리검증*/ COUNT(*) AS CNT, 'Y' AS Y
          FROM (${extc_qury}) AS A
    </select>
</mapper>
